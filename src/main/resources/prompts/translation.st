Translate the following JSON object into {targetLang}.

This JSON represents data from the Horizon video game universe (Horizon Zero Dawn, Horizon Forbidden West).
Translate only natural-language descriptions or readable text, 
while preserving all field names, IDs, slugs, enums, and structure exactly as is.

Do not translate proper names of machines, characters, locations, tribes, or games 
(e.g. "Aloy", "GAIA", "Thunderjaw", "Horizon Forbidden West").

If a value is "No data", translate it naturally into the target language (e.g. "Sin datos").

JSON:
{json}
